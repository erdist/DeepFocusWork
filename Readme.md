1. Create virtual environment on base directory and activate it.
2. Run the following command: pip3 install -r requirements.txt
3. Finally, run to execute project on localhost:
	python3.x application.py
